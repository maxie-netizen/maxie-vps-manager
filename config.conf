# Maxie VPS Manager Configuration File

# Protocol Ports
BADVPN_PORT=7300
UDP_CUSTOM_PORT=5300
SSL_TUNNEL_PORT=444
WEBSOCKET_PORT=8080
SOCKS_PORT=200
DNSTT_PORT=53
SSLH_PORT_HTTP=80
SSLH_PORT_HTTPS=443

# SSH Configuration
SSH_PORT=22

# Firewall Settings
FIREWALL_ENABLE=true

# Logging
LOG_LEVEL=INFO
LOG_FILE=/var/log/maxie-vps-manager.log

# Backup Settings
BACKUP_RETENTION_DAYS=30
BACKUP_DIR=/backup/users

# SSL Settings
SSL_AUTO_RENEW=true
SSL_RENEWAL_CRON="0 12 * * *"

# User Management
USER_EXPIRY_DEFAULT_DAYS=30
USER_MAX_CONNECTIONS=1000
USER_BANDWIDTH_LIMIT_MB=1024

# Security Settings
SSH_KEY_AUTH_ONLY=false
SSH_ROOT_LOGIN=false
SSH_PASSWORD_AUTH=true
